{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests with oj-tools",
            "type": "shell",
            "command": "source ${workspaceFolder}/.venv/bin/activate && cd ${relativeFileDirname} && oj t -c \"python ${fileBasename}\"",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}